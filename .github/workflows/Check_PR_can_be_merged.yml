name: Check PR can be merged
on:
  issue_comment:
    types: [created]
jobs:
  merge:
    if: contains(github.event.comment.body, 'Success') 
    runs-on: ubuntu-latest
    steps:
    - run: |
        echo PR #${{ github.event.number }} has been merged
